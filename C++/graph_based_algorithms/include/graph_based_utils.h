#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <vector>
#include <utility> 
#include <set> 
#include <queue>

void bfs(const std::vector<std::vector<char>>& map);// Replace with your actual function signature
std::pair<std::pair<int, int>, std::pair<int, int>> findLocations(const std::vector<std::vector<char>>& map)

#endif
